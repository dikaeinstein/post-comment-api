sudo: false
dist: trusty
language: node_js
node_js:
  - 10.15.0
services:
  - mongodb
cache: yarn
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.16.0
  - export PATH="$HOME/.yarn/bin:$PATH"
before_script:
  - yarn install
script:
  - yarn lint
  - yarn test
